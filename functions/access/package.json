{
  "name": "reposell-listing-access",
  "version": "0.2.0",
  "private": true,
  "description": "WorkOS AuthKit (GitHub login) + discovery-access processing for the official reposell listing. Runs as a Cloudflare Worker with D1.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}